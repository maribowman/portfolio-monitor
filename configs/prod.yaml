server:
  port: 9876
  mode: release
signal:
  linkedDevice: ${portfolio-monitor.signal.linkedDevice}
coinbase:
  apiKey: ${portfolio-monitor.coinbase.apiKey}
  apiSecret: ${portfolio-monitor.coinbase.apiSecret}
